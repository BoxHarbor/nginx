# Default server configuration
# BoxHarbor Nginx

server {
    # Listen on non-privileged port for rootless
    listen 8080 default_server;
    listen [::]:8080 default_server;
    
    # Server name
    server_name _;
    
    # Document root
    root /web;
    index index.html index.htm;
    
    # Charset
    charset utf-8;
    
    # Logging
    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log warn;
    
    # Main location
    location / {
        try_files $uri $uri/ =404;
    }
    
    # Deny access to hidden files
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
    
    # Favicon and robots.txt
    location = /favicon.ico {
        log_not_found off;
        access_log off;
    }
    
    location = /robots.txt {
        log_not_found off;
        access_log off;
    }
    
    # Cache static assets
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
    
    # Error pages
    error_page 404 /404.html;
    error_page 500 502 503 504 /50x.html;
    
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}

# Example: HTTPS configuration (commented out by default)
# Uncomment and configure when you have SSL certificates

# server {
#     listen 8443 ssl http2;
#     listen [::]:8443 ssl http2;
#     
#     server_name example.com;
#     
#     # SSL Configuration
#     ssl_certificate /config/ssl/cert.pem;
#     ssl_certificate_key /config/ssl/key.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     
#     root /web;
#     index index.html index.htm;
#     
#     location / {
#         try_files $uri $uri/ =404;
#     }
# }